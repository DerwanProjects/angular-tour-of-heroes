import { ArticleLengthDirective } from './article-length.directive';

describe('ArticleLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new ArticleLengthDirective();
    expect(directive).toBeTruthy();
  });
});
