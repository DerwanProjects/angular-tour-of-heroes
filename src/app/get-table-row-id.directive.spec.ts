import { GetTableRowIdDirective } from './get-table-row-id.directive';

describe('GetTableRowIdDirective', () => {
  it('should create an instance', () => {
    const directive = new GetTableRowIdDirective();
    expect(directive).toBeTruthy();
  });
});
