import { FontLgDirective } from './font-lg.directive';

describe('FontLgDirective', () => {
  it('should create an instance', () => {
    const directive = new FontLgDirective();
    expect(directive).toBeTruthy();
  });
});


